services:
  - type: web
    name: vitalia-juanpito-backend
    env: python
    branch: main
    rootDir: PYTHON
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
