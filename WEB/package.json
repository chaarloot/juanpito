{
  "name": "vitalia-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs'); const api=process.env.API_URL||'https://vitalia-juanpito-backend.onrender.com'; const content='window.API_URL = ' + JSON.stringify(api) + ';\\n'; fs.writeFileSync('config.js', content); fs.writeFileSync('../config.js', content);\""
  }
}
